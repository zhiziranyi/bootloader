# FlashSafe Pro — 带安全签名与网络升级的工业级 Bootloader

## 项目名称

**FlashSafe Pro — 基于 STM32F407 的安全网络 OTA Bootloader**

## 项目功能

本项目在四分区高可靠架构基础上，新增两大工业级特性：

- **安全签名验签**：基于 SHA-256 + ECDSA（secp256r1 曲线）的数字签名机制，使用 mbedTLS 库实现。服务器持有私钥签名，Bootloader 内置公钥验签，防止恶意固件刷入
- **网络升级通道**：基于 LWIP + HTTP 协议，支持从 HTTP 服务器下载固件包，并实现 HTTP Range 断点续传，网络中断后可恢复下载
- **四分区存储架构**：Bootloader + App_A + App_B + Config，支持乒乓升级（A/B 面切换）
- **外部 Flash 协处理**：W25Q64 作为固件下载缓存区和出厂固件备份区
- **掉电恢复机制**：基于状态机的断点续传，任意时刻断电重启后可恢复或回滚
- **多级回滚策略**：App_A 损坏切 App_B，全部损坏则从 Factory 区恢复
- **Python 打包与签名工具**：一键生成签名固件包

---

## 一、硬件连接方案

### STM32F407ZGT6 引脚分配

| 外设 | 引脚 | 连接对象 | 备注 |
|:---|:---|:---|:---|
| **USART1 TX** | PA9 | CH340 RXD | 日志输出，115200bps |
| **USART1 RX** | PA10 | CH340 TXD | CLI 指令接收 |
| **LED** | 板载LED引脚 | — | 状态指示 |
| **SPI2 SCK** | PB13 | W25Q64 CLK | |
| **SPI2 MISO** | PB14 | W25Q64 DO | |
| **SPI2 MOSI** | PB15 | W25Q64 DI | |
| **SPI2 CS** | PB12 | W25Q64 CS | 软件控制片选 |
| **ETH MDIO** | PA2 | LAN8720 MDIO | RMII 接口 |
| **ETH MDC** | PC1 | LAN8720 MDC | |
| **ETH RMII REF CLK** | PA1 | LAN8720 50MHz CLK | |
| **ETH RMII CRS DV** | PA7 | LAN8720 CRS_DV | |
| **ETH RMII RXD0** | PC4 | LAN8720 RXD0 | |
| **ETH RMII RXD1** | PC5 | LAN8720 RXD1 | |
| **ETH RMII TX EN** | PB11 | LAN8720 TXEN | |
| **ETH RMII TXD0** | PB12 | LAN8720 TXD0 | 注意与 SPI2 CS 冲突 |
| **ETH RMII TXD1** | PB13 | LAN8720 TXD1 | 注意与 SPI2 SCK 冲突 |
| **ETH RESET** | PD3 | LAN8720 nRST | 复位引脚 |

### 引脚冲突解决方案

ETH 和 SPI2 存在引脚冲突（PB12、PB13），有两种解决方案：

**方案 A（推荐）**：将 W25Q64 迁移到 SPI3
- SPI3 SCK → PC10
- SPI3 MISO → PC11
- SPI3 MOSI → PC12
- SPI3 CS → PD2（软件控制）

**方案 B**：SPI2 和 ETH 分时复用
- 下载阶段使用 ETH，W25Q64 不操作
- 搬移阶段断开 ETH，使用 SPI2 操作 W25Q64
- 复杂度较高，不推荐

> **本方案采用方案 A**，将 W25Q64 挂载到 SPI3。

### 购买清单（新增部分）

| 物品 | 推荐型号 | 数量 | 预算参考 | 用途 |
|:---|:---|:---|:---|:---|
| **以太网模块** | LAN8720 ETH 模块（RMII 接口） | 1 | ¥15-25 | 网络通信 |
| **网线** | 超五类网线 | 1 | ¥5-10 | 连接路由器 |
| **路由器/交换机** | 任意家用路由器 | 1 | 已有 | 局域网环境 |

### 修正后的完整引脚分配

| 外设 | 引脚 | 连接对象 | 备注 |
|:---|:---|:---|:---|
| **USART1 TX** | PA9 | CH340 RXD | 日志输出，115200bps |
| **USART1 RX** | PA10 | CH340 TXD | CLI 指令接收 |
| **LED** | 板载LED引脚 | — | 状态指示 |
| **SPI3 SCK** | PC10 | W25Q64 CLK | 原 SPI2 迁移 |
| **SPI3 MISO** | PC11 | W25Q64 DO | |
| **SPI3 MOSI** | PC12 | W25Q64 DI | |
| **SPI3 CS** | PD2 | W25Q64 CS | 软件控制片选 |
| **ETH MDIO** | PA2 | LAN8720 MDIO | RMII |
| **ETH MDC** | PC1 | LAN8720 MDC | |
| **ETH REF CLK** | PA1 | LAN8720 CLK | 50MHz |
| **ETH CRS DV** | PA7 | LAN8720 CRS_DV | |
| **ETH RXD0** | PC4 | LAN8720 RXD0 | |
| **ETH RXD1** | PC5 | LAN8720 RXD1 | |
| **ETH TX EN** | PB11 | LAN8720 TXEN | |
| **ETH TXD0** | PB12 | LAN8720 TXD0 | |
| **ETH TXD1** | PB13 | LAN8720 TXD1 | |
| **ETH RESET** | PD3 | LAN8720 nRST | |

### 注意事项

- W25Q64 的 WP 和 HOLD 接 3.3V
- LAN8720 需要 50MHz 有源晶振，模块一般自带
- SD 卡使用板载接口，无需额外接线
- 所有模块共地

---

## 二、分布实现步骤

### 阶段1：基础工程搭建（第1-2天）

#### 1.1 CubeMX 配置

- 芯片：STM32F407ZGT6
- 时钟：HSE + PLL → 168MHz
- 使能 USART1：异步模式，115200bps
- 使能 SPI3：全双工主机，PC10=SCK, PC11=MISO, PC12=MOSI，软件 CS(PD2)
- 使能 SDIO：4线模式
- 使能 FatFs：关联 SDIO
- **使能 ETH**：RMII 模式，引脚按上表配置
- **使能 LWIP**：关联 ETH，使用 DHCP 获取 IP
- 生成 MDK/Keil 工程

#### 1.2 网络验证

- 初始化 ETH + LWIP
- 实现 `eth_init()` 函数，等待 DHCP 获取 IP
- 实现 `eth_get_ip()` 打印当前 IP 地址
- Ping 测试验证网络通断

**测试**：上电后串口打印分配的 IP 地址，电脑能 Ping 通

#### 1.3 基础外设验证

- 重定向 `printf` 到 USART1
- LED 闪烁
- W25Q64 读 ID 测试
- SD 卡挂载测试

---

### 阶段2：mbedTLS 移植与安全模块（第3-5天）

#### 2.1 mbedTLS 移植

- 从 [mbedTLS GitHub](https://github.com/Mbed-TLS/mbedtls) 下载最新版本
- 仅保留所需文件，最小化体积：
  - `sha256.c/h`
  - `ecdsa.c/h`
  - `ecp.c/h`
  - `bignum.c/h`
  - `asn1parse.c/h` 及相关依赖
- 配置 `mbedtls_config.h`，裁剪不需要的功能模块
- 添加到 Keil 工程，添加头文件路径
- 编写 `mbedtls_init.c`，配置内存分配函数

**测试**：调用 SHA-256 计算一段测试数据，与在线工具对比结果一致

#### 2.2 ECDSA 签名验证函数

新建 `secure_boot.c` 和 `secure_boot.h`，实现：

- 定义公钥结构体，使用 secp256r1（NIST P-256）曲线
- `secure_verify_firmware(fw_header_t *hdr, uint8_t *firmware_data, uint32_t fw_size, uint8_t *signature, uint32_t sig_len)` — ECDSA 验签
- 签名数据结构体 `signature_block_t`：
  - `uint32_t sig_len` — 签名长度
  - `uint8_t signature[64]` — DER 编码的签名
  - `uint32_t sig_crc` — 签名块自身 CRC
- `secure_get_pubkey_hash(uint8_t *hash)` — 计算公钥的 SHA-256（用于防篡改校验）

#### 2.3 固件包结构升级

新固件包结构：
[fw_header_t] → 固件头（含版本、大小、CRC）
[firmware body] → 固件体
[signature_block_t] → 签名块（ECDSA 签名）

text

升级后的固件头：

```c
typedef struct {
    uint32_t magic;           // 0x464C4153
    uint16_t ver_major;
    uint16_t ver_minor;
    uint32_t image_size;      // 固件体大小
    uint32_t image_crc;       // 固件体 CRC32
    uint8_t  pubkey_hash[32]; // 公钥 SHA-256，防篡改
    uint32_t signature_offset; // 签名块在文件中的偏移
    uint32_t header_crc;      // 自身 CRC
} fw_header_t;
2.4 验签流程
text
bootloader_verify_and_install():
  1. 读取固件头，校验 header_crc
  2. 校验 pubkey_hash == 内置公钥的 SHA-256（防公钥被替换）
  3. 读取固件体，校验 image_crc
  4. 根据 signature_offset 读取签名块
  5. 计算固件头+固件体的 SHA-256
  6. 使用内置公钥验证 ECDSA 签名
  7. 验签通过 → 允许安装
  8. 验签失败 → 丢弃固件，报安全错误
2.5 密钥生成与签名工具
新建 Tools/gen_keypair.py：

使用 Python ecdsa 库生成 secp256r1 密钥对

私钥保存为 private_key.pem（服务器使用，严格保密）

公钥导出为 C 数组格式，嵌入 Bootloader 代码

升级 Tools/pack_firmware.py：

新增参数：--key private_key.pem 指定私钥路径

新增流程：

读取 bin 文件
计算固件体 CRC32
填充固件头（含公钥哈希）
计算固件头 CRC
对固件头+固件体做 SHA-256
用私钥签名
将签名块追加到文件末尾
输出最终 pkg 文件
测试：生成密钥对 → 打包签名固件 → Bootloader 验签通过 → 故意篡改 pkg → 验签失败

阶段3：分区架构与配置区（第6天）
与基础版本一致，此处列出要点，详细步骤参见阶段3原文。

3.1 分区表（partition.h）
c
#define ADDR_BOOTLOADER   0x08000000
#define SIZE_BOOTLOADER   (80 * 1024)   // 增大到 80KB，容纳 mbedTLS

#define ADDR_APP_A        0x08014000    // 相应后移
#define SIZE_APP_A        (432 * 1024)

#define ADDR_APP_B        0x08080000
#define SIZE_APP_B        (448 * 1024)

#define ADDR_CONFIG       0x080F0000
#define SIZE_CONFIG       (16 * 1024)

#define EXT_DOWNLOAD      0x000000
#define EXT_FACTORY       0x200000
注意：引入 mbedTLS 后 Bootloader 体积增大，Flash 布局需重新计算。

3.2-3.3 配置区结构体与上电初始化
与基础版本一致，boot_config_t 结构体不变

config_read/write/set_default/print 实现不变

阶段4：HTTP 网络升级通道（第7-9天）
4.1 HTTP 客户端实现
新建 http_client.c 和 http_client.h，实现：

http_init() — 初始化 LWIP 网络栈

http_get_file_size(url) — 发送 HTTP HEAD 请求，解析 Content-Length

http_download_range(url, start_byte, end_byte, buf, len) — HTTP Range GET 请求，下载指定字节范围

http_download_full(url, callback) — 全量下载，边下载边回调写入 W25Q64

HTTP 请求头格式：

text
GET /firmware/firmware_v1.1.pkg HTTP/1.1
Host: 192.168.1.100:8080
Range: bytes=0-1023
Connection: close
4.2 断点续传实现
新建 download.c 和 download.h：

download_resume() — 断点续传主函数：

从配置区读取上次下载进度（已下载字节数）
构造 HTTP Range 请求，从断点继续下载
每下载一个数据块（如 4KB）：
写入 W25Q64 Download 区对应位置
更新配置区下载进度
计算 CRC32（增量更新）
下载完成 → 校验 CRC32
下载失败 → 记录进度，下次从断点继续
download_get_progress() — 返回当前下载百分比

4.3 固件服务器搭建
方案 A（推荐）：Python 简易 HTTP 服务器

Tools/fw_server.py：

使用 http.server 模块

支持 Range 请求

指定固件目录路径

运行：python fw_server.py --port 8080 --dir ./firmware/

方案 B：使用 Nginx/Apache

配置静态文件服务，启用 Range 支持（默认支持）

测试：电脑浏览器访问 http://开发板IP:8080/firmware/ 验证连通

4.4 网络升级流程集成
升级 upgrade.c，新增网络升级路径：

text
upgrade_check_and_run():
  1. 挂载 SD 卡文件系统
  2. 扫描 SD 卡 firmware.pkg
  3. 扫描 SD 卡 upgrade_url.txt（内含固件服务器 URL）
  4. 优先级：
     - SD 卡有 pkg → 本地升级流程
     - SD 卡有 url → 网络升级流程
     - 均无 → 直接跳转 App
  5. 网络升级流程：
     a. 初始化网络，等待 DHCP
     b. HTTP GET 查询固件版本（服务器端需提供 version.json）
     c. 对比版本号，相同则跳过
     d. HTTP HEAD 获取文件大小
     e. 调用 download_resume() 断点续传下载
     f. 下载完成 → 验签 → 搬移到 App_B
  6. 跳转
4.5 调试 CLI 命令
在 USART1 上实现简易命令行接口（cli.c）：

支持命令：

info — 打印当前版本、分区状态

upgrade sd — 手动触发 SD 卡升级

upgrade net <url> — 手动触发网络升级

rollback — 手动回滚到另一分区

factory — 恢复出厂固件

status — 查看下载进度

阶段5：升级流程状态机增强（第10天）
5.1 升级状态定义
c
typedef enum {
    UPGRADE_IDLE,           // 无升级任务
    UPGRADE_CHECKING,       // 正在检查版本
    UPGRADE_DOWNLOADING,    // 正在网络下载
    UPGRADE_DOWNLOADED,     // 下载完成，存于 W25Q64
    UPGRADE_VERIFYING,      // 正在验签
    UPGRADE_VERIFIED,       // 验签通过
    UPGRADE_SWAPPING,       // 正在搬移到 App 区
    UPGRADE_DONE,           // 升级完成
    UPGRADE_FAILED,         // 升级失败（安全错误/网络错误）
    UPGRADE_ROLLBACK        // 正在回滚
} upgrade_state_t;
5.2 掉电恢复增强
每个状态切换立即写配置区

上电恢复逻辑：

CHECKING → 重新检查

DOWNLOADING → 从断点继续下载

DOWNLOADED → 开始验签

VERIFYING → 重新验签

VERIFIED → 开始搬移

SWAPPING → 检查 App_B，继续搬移

FAILED → 触发回滚

5.3 安全错误处理
验签失败 → 记录失败次数，超过阈值（如3次）则锁定，等待手动恢复

防止暴力攻击：连续验签失败后延迟重试时间指数增长

阶段6：App 工程与跳转（第11天）
6.1 App 工程配置
Flash 起始地址：0x08014000（根据 Bootloader 实际大小调整）

SCB->VTOR = 0x08014000

使能 USART1，打印版本信息

使能 ETH + LWIP（App 可能也需要网络）

编译生成 app.bin

6.2 Bootloader 跳转函数
与基础版本一致：

校验栈指针在 RAM 范围内

关全局中断

反初始化所有外设（ETH、SPI3、SDIO、USART1）

设置 VTOR

设置 MSP

跳转

特别注意：ETH 的 DMA 描述符必须在跳转前释放，否则 App 初始化 ETH 会冲突。

6.3 联调测试
烧录 Bootloader

搭建固件服务器

SD 卡放入 upgrade_url.txt，内容为 http://192.168.1.100:8080/firmware/firmware_v1.1.pkg

上电，观察网络升级全流程

下载过程中拔网线，重新上电验证断点续传

用错误签名的固件测试，验证验签拒绝

阶段7：异常测试与安全性验证（第12天）
7.1 功能测试
测试场景	操作	预期结果
正常网络升级	服务器放签名固件，上电	自动下载、验签、升级
断点续传	下载50%拔网线，重新上电	从50%继续下载
验签通过	正确签名的固件	允许安装
验签失败	篡改过的固件	拒绝安装，报安全错误
签名缺失	无签名块的固件	拒绝安装
公钥不匹配	另一对密钥签名的固件	拒绝安装
SD 卡本地升级	SD 卡放 pkg	走本地升级流程
版本相同	服务器版本与当前相同	跳过升级
网络不通	服务器不可达	报网络错误，保持旧版本
7.2 安全测试
测试场景	操作	预期结果
重放攻击	重放旧版本签名固件	版本号相同则跳过
降级攻击	强制刷入低版本固件	版本号比较，可选禁止降级
中间人篡改	传输中修改固件数据	CRC 或验签失败
暴力验签	连续发送错误签名固件	触发锁定机制
三、最终工程结构
text
FlashSafe_Pro/
├── Bootloader/                    # Bootloader Keil 工程
│   ├── Core/
│   │   ├── main.c                 # 主逻辑
│   │   ├── config.c/.h            # 配置区读写
│   │   ├── jump.c/.h              # App 跳转
│   │   ├── upgrade.c/.h           # 升级状态机
│   │   ├── cli.c/.h               # 命令行接口
│   │   └── partition.h            # 分区表
│   ├── Drivers/
│   │   ├── w25q64.c/.h            # SPI Flash 驱动 (SPI3)
│   │   ├── flash_internal.c/.h    # 内部 Flash 驱动
│   │   ├── crc32.c/.h             # CRC32
│   │   └── eth_init.c/.h          # 以太网初始化
│   ├── Network/
│   │   ├── http_client.c/.h       # HTTP 客户端
│   │   └── download.c/.h          # 断点续传管理
│   ├── Security/
│   │   ├── secure_boot.c/.h       # 验签逻辑
│   │   └── mbedtls/               # mbedTLS 裁剪版
│   └── Middleware/
│       └── LWIP/                  # LWIP 协议栈（CubeMX 生成）
├── App/                           # App 独立工程
│   └── Core/main.c
├── Tools/
│   ├── gen_keypair.py             # 密钥对生成工具
│   ├── pack_firmware.py           # 固件打包+签名工具
│   └── fw_server.py               # 简易固件服务器
├── Firmware/                      # 固件存放目录
│   ├── version.json               # 版本信息
│   └── firmware_v1.x.pkg          # 签名固件包
└── README.md
四、关键技术要点
4.1 Bootloader 体积控制
mbedTLS 必须裁剪，仅保留 ECDSA + SHA-256 + ASN.1 解析

编译优化开 -Os（体积优先）

目标：Bootloader 控制在 80KB 以内

4.2 签名安全性
私钥由服务器持有，绝不写入固件

公钥嵌入 Bootloader 代码段（只读），并计算自身哈希防篡改

固件头包含 pubkey_hash，防止固件包内嵌恶意公钥

4.3 HTTP Range 断点续传
依赖服务器支持 Range 头（Nginx/Apache/Python 均支持）

进度数据存入配置区，每次写入后更新

W25Q64 Download 区从地址 0 开始顺序写入，断点续传直接追加

4.4 ETH 与 SPI3 的 DMA 冲突
ETH 使用 DMA 通道，SPI3 也尽量使用 DMA

跳转前务必反初始化 ETH DMA，否则 App 端 ETH 无法正常工作